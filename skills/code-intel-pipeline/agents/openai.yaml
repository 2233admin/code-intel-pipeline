interface:
  display_name: "Code Intel Pipeline"
  short_description: "Install and run repository intelligence gates"
  default_prompt: "Use $code-intel-pipeline to install or validate the pipeline and analyze the current repository."
