[CmdletBinding(DefaultParameterSetName="Rehearsal")]param([Parameter(Mandatory=$true,ParameterSetName="Apply")][string]$TargetPath,[Parameter(Mandatory=$true,ParameterSetName="Rehearsal")][string]$RehearsalRoot,[string]$RepoRoot=(Split-Path (Split-Path $PSScriptRoot -Parent) -Parent),[string]$SourceRevision="working-tree-bounded-source")
Set-StrictMode -Version Latest;$ErrorActionPreference="Stop";$live=Join-Path $RepoRoot "update-code-intel-index.ps1";$source=[IO.File]::ReadAllText($live);$hash=(Get-FileHash $live -Algorithm SHA256).Hash.ToLowerInvariant();$pattern='(?s)function Read-JsonFile \{.*\z';if([regex]::Matches($source,$pattern).Count-ne1){throw "current snapshot lacks exact E10 branch"};$deleted=[regex]::Replace($source,$pattern,"",1)
if($PSCmdlet.ParameterSetName-eq"Rehearsal"){if(Test-Path $RehearsalRoot){throw "rehearsal root must be exclusive"};New-Item -ItemType Directory $RehearsalRoot|Out-Null;$TargetPath=Join-Path $RehearsalRoot "update-code-intel-index.ps1";[IO.File]::WriteAllText($TargetPath,$deleted,[Text.UTF8Encoding]::new($false))}
$target=[IO.Path]::GetFullPath($TargetPath);if([IO.File]::ReadAllText($target)-ne$deleted){throw "rollback target is not exact E10 deletion result"};[IO.File]::WriteAllText($target,$source,[Text.UTF8Encoding]::new($false));$targetHash=(Get-FileHash $target -Algorithm SHA256).Hash.ToLowerInvariant();if($targetHash-ne$hash){throw "E10 rollback is not byte exact"};if((Get-FileHash $live -Algorithm SHA256).Hash.ToLowerInvariant()-ne$hash){throw "live index facade changed"}
[ordered]@{schema="code-intel-compatibility-rollback-rehearsal.v1";branchId="update-code-intel-index.legacy-compatibility-traversal";target=$target;sourceRevision=$SourceRevision;rehearsal=($PSCmdlet.ParameterSetName-eq"Rehearsal");exactReplay=$true;sourceSha256=$hash;targetSha256=$targetHash;changedFiles=@($target);a08Changed=$false;e05Changed=$false;liveFacadeChanged=$false}|ConvertTo-Json -Compress
